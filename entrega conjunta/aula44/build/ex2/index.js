function soma(x, y) {
    return (x + y);
    console.log(soma);
}
function subtrai(x, y) {
    return (x - y);
    console.log(subtrai);
}
function mult(x, y) {
    return (x * y);
    console.log(mult);
}
function maior(x, y) {
    let diferença = x - y;
    diferença > 0 ? console.log(x + "> " + y) : console.log(x + "< " + y);
}
//# sourceMappingURL=index.js.map